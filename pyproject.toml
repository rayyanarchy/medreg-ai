[project]
name = "medreg-ai"
version = "0.1.0"
description = "A RAG system for asking natural-language questions against medical regulatory documents"
readme = "README.md"

requires-python = ">=3.13"
dependencies = [
    "chromadb>=1.5.9",
    "openai>=3.19.2",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[tool.uv]
package = false
